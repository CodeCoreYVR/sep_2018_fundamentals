/**
 * comments with multiple lines
 */

// comments with single lines

/**
 * We learnt how to define a variable
 */

const myFirstConstantVariable = 'My first inmutable constnant';
let myFirstVariable = `My first mutable variable`;
var myFirstGlobalVariable = "My first globale variable"

// How to compare variables by checking its value only
2 == '2'; // true

// How to compare variables by checking both its type
// and its value

2 === '2'; // not true

// if statements
let arturos_age = 35;
if(arturos_age == 35) {
  console.log('boy you are old as f.....');
}

for( var index = 0; index < 10; index++ ) {
  console.log(`the value of index is ${index}`);
}

